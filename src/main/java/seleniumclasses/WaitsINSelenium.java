package seleniumclasses;

public class WaitsINSelenium {

	//there are two waits
	//1.Static wait-->Thread.sleep()-it pause the script for specified time it doesnt matter if the elmnt found before that time
	//2.Dynamic wait-->if the elmnt found before specified time the rest of the time will be ignored
	     //2.1:Implicit wait--global wait--applicable for all web elmts by default.
	     //2.2:Explicit wait
	          //2.2.1: WebDriver wait
	          //2.2.2: Fluent Wait
	
	//WebDriverWait(class) extends FluentWait(class) implements Wait(Interface) has a method
	//until()
}
